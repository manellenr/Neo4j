{
  "iri": {
    "_iri": "https://www.manellenr.fr",
    "graph": {
      "_nodes": [
        { "id": "cnf", "label": "CNF", "properties": { "name": "UPF" } }
      ],
      "_relationships": [
        { "from": "cnf", "to": "lbGroup", "type": "CONTAINS" }
      ]
    }
  }
}


components:
  schemas:
    Graph:
      type: object
      properties:
        _iri:
          type: string
          format: uri
        graph:
          type: object
          properties:
            _nodes:
              type: array
              items:
                $ref: '#/components/schemas/Node'
            _relationships:
              type: array
              items:
                $ref: '#/components/schemas/Relationship'

    Node:
      type: object
      properties:
        id:
          type: string
        label:
          type: string
        properties:
          type: object
          additionalProperties:
            type: string

    Relationship:
      type: object
      properties:
        from:
          type: string
        to:
          type: string
        type:
          type: string
